function srb_karanlik_numaralar() { //srb_kararma() ile kullanılır.
	if global.karanlik >= 1 {
		srb_karanlik_tumu();
		switch (global.hikaye_kontrolleri_64) {
		    case 1: // Karanlık numarası 64
		        
		        break;
		}
	}	
}